{
  "name": "@finogeek/cxagent",
  "version": "1.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "cxagent": "dist/index.js"
  },
  "scripts": {
    "dev": "bun run index.ts",
    "start": "bun run dist/index.js",
    "build": "bun tsc && bun run build:assets && chmod +x dist/index.js",
    "build:assets": "cp brain.md dist/ && mkdir -p dist/conf && cp -r conf/* dist/conf/ && cp .agent.env dist/ 2>/dev/null || true",
    "build:web": "cd web && bun install && bun run build",
    "build:all": "bun run build && bun run build:web",
    "test": "vitest run",
    "test:watch": "vitest",
    "watch:ts": "bun tsc-watch",
    "watch": "bun concurrently \"bun run watch:ts\"",
    "clean": "rm -rf dist",
    "clean:all": "bun run clean && rm -rf web/dist web/node_modules",
    "rebuild": "bun run clean && bun run build",
    "rebuild:all": "bun run clean:all && bun run build:all"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "files": [
    "dist",
    "!dist/**/*.map",
    "web/dist/finclip-chat.js",
    "web/dist/assets/**"
  ],
  "dependencies": {
    "@finogeek/actgent": "^1.0.4",
    "commander": "^12.1.0",
    "dotenv": "^16.4.7", 
    "uuid": "^9.0.1",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "bun-types": "^1.1.38",
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
